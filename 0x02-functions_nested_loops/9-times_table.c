#include "main.h"
/**
 * times_table - print multiplication table
 * Return: 0
 */
void times_table(void)
{
	int n, m;

	if ((n = 0; n <= 9; n++) && (m = 0; m <= 9; m++))
	{
		_putchar((n * m) + '0');
		_putchar('\n');
		return (0);
	}
}
