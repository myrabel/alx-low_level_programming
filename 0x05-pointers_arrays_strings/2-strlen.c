#include "main.h"
#include "string.h"
#include <stdio.h>
/**
 * _strlen - return length of string
 * @s: character
 * Return: Always 0
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
