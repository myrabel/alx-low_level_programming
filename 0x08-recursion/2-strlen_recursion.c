#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - return length of a string
 * @s: variable
 * Return: length of string
 */

int _strlen_recursion(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
