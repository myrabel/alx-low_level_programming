#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @argc: arguments count
 * @argv: arguments vector
 * Return: result
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
